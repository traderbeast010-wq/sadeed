# سديد (Sadeed) — تبعيات خادم بايثون. بلا أي حزمة ذكاء اصطناعيّ خارجية:
# التوليد والتضمين عبر llama.cpp محلياً (خادم HTTP على 127.0.0.1).
fastapi>=0.110
uvicorn[standard]>=0.29
python-multipart>=0.0.9   # رفع الملفات (multipart/form-data)
numpy>=1.26
pymupdf>=1.24             # استخراج نصّ PDF
python-docx>=1.1          # استخراج نصّ Word
